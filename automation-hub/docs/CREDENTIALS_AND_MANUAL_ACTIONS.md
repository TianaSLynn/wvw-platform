# Credentials and Manual Actions Needed

| Item | Needed for | Status | Action needed from Tiána |
|---|---|---|---|
| ~~Wave Pro API access~~ | Verifying payment status against the existing public Wave payment link (`https://link.waveapps.com/uun3sr-jm72jd`) and reconciling it to registrations | **Resolved 2026-08-04.** Tiána provided a Wave "Full Access" business API token (from Wave's own Settings > API page, not an OAuth app). Verified live against the real business and stored as an encrypted `WAVE_API_TOKEN` env var on the `wvw-automation-hub` Netlify site — never committed to this repo. `packages/integration-wave/src/client.ts` built and dev-tested against the live API. **Confirmed by Tiána (2026-08-04): keep using the existing public Wave web pay link as the payment collection UX — no custom checkout needs to be built.** Wave API access is used for *verifying* payment/invoice status against that link (Wave stays the authoritative source of payment truth; a redirect never counts as proof of payment). | None — reconciliation automation using this client still needs to be built |
| ~~Netlify Personal Access Token~~ | Creating an outgoing webhook notification on a live Netlify form so real submissions reach the deployed `wvw-automation-hub` intake function | **Resolved 2026-08-07.** The "WVW Automation" OAuth app's authorize link 404'd (`invalid redirect_uri parameter` — the app's registered redirect URI didn't match what we tried). Tiána generated a Netlify **Personal Access Token** instead (User settings → Applications → Personal access tokens), which worked. Used once, ephemerally, to create a site-wide outgoing webhook (hook id `6a755e753c5b34ea395ddcff`) on `wholisticvibeswellness` pointed at `https://wvw-automation-hub.netlify.app/.netlify/functions/intake`. Confirmed via the Hooks API listing — sits alongside the pre-existing email notification hook, doesn't replace it. Token was never written to any file or committed. | None for the webhook itself. Still blocked on Decision 10 below (canonical form set) before this webhook meaningfully does anything with real registrations |
| Neon Postgres access | Persistence layer for `automation_events`/`workflow_executions`/etc. — the original target was Supabase, which remains paused and blocked (see below) | **New 2026-08-04**: Tiána provided a Neon API token, which revealed two already-provisioned live Postgres 17 databases (`wvw-platform`, `wvw-intelligence`, org `org-cold-bird-95369003`) that predate this session and have sat unused since. This could replace Supabase as the persistence layer — the drafted migrations are portable SQL. Retrieving the actual connection string (which includes a password) was blocked by the same system safety classifier that blocks Supabase project restores. | Either grant Bash permission for connection-string retrieval, or pull the connection string yourself from the Neon console (Project → Connection Details) and provide it directly so it can be stored as a Netlify env var — never committed |
| Apollo.io | Any real Apollo integration (org/people search, enrichment) | **Connected 2026-08-07** via this session's Apollo MCP connector. Not yet used for anything — no Apollo task has come up in this thread of work | None yet |
| Repo creation permission, or a manually-created empty `wvw-automation-hub` repo | Giving the hub its own repository instead of living inside `wvw-platform` | Blocked — 403 from GitHub App | Create the repo yourself and tell me to attach it, or authorize repo-creation scope for this integration |
| Supabase project selection + resume approval | Applying the drafted schema migrations; any live automation_events/exceptions tracking | Both candidate projects (`wvw-command-center`, `WVW Dashboard`) paused | Confirm which project to use and approve resuming it (may affect billing) |
| ~~Working `NOTION_API_KEY`~~ | Letting the intake function actually persist registrations to Notion instead of only dry-running | **Resolved 2026-08-07.** Tiána created a dedicated integration ("WVW Automation," owned by her user, not the workspace) with Read/Update/Insert content capability, shared with the relevant pages. Verified live: full read (`/search`) and write (create + archive a real throwaway test page, id `3b5b0f7a-44f3-813d-b691-f15281018fe9`, cleaned up immediately) both work. Stored as `NOTION_API_KEY` on the `wvw-automation-hub` Netlify site, encrypted, never committed. A second, earlier "WVW Academy" workspace-owned integration also tested valid with full read/write — not used, to keep one dedicated credential per service | None for the credential itself. **Flipping any feature flag to actually go live is a separate decision** — not done yet, needs explicit per-path approval per ADR-001 |
| Notion database IDs for TRAIN-01, TRAIN-06, TRAIN-07, TRAIN-12, TRAIN-14, TRAIN-17, TRAIN-18, TRAIN-19, COMMS-02, COMMS-06 | Notion integration package (governance forbids inventing IDs) | Pages found and readable; individual database IDs not yet all confirmed | Confirm or point me to the MHFA-40 Forms & Intake Center / TRAIN-12 registry, which appears to already list these |
| ~~Confirmation of canonical form set~~ | Building the intake function against the right live schema | **Resolved 2026-08-07.** Tiána confirmed `FORM-MHFA-001` is the one to use — consistent with the evidence (real submission 2026-08-02 vs. zero ever for `mhfa-individual-registration`). Its handler already has a real Notion mapper wired in. `mhfa-individual-registration`'s schema is being kept in place (not deleted) in case it's used somewhere not yet found, but is not the priority path | None. **Still open: whether to flip `FORM-MHFA-001`'s feature flag and let real registrations actually persist to Notion** — asked explicitly, not yet answered |
| MHFA-01A incident status | Safe cutover of MHFA-REG-01 | Last evidence found (2026-07-30) shows 1 of 5 records still unresolved | Confirm current status |
| Microsoft Graph app registration model (delegated vs. application permissions) | Outlook draft creation, mailbox search | Session has a working delegated Graph connection (`hello@wholisticvibeswellness.com`) via MCP, but this is the *session's* connection, not necessarily what a deployed Netlify/Supabase Edge Function would use in production | Confirm whether production email automation should reuse this delegated identity or need its own app registration — this is explicitly a rule-27 "document options, don't silently choose a high-privilege model" item |

## Never requested

Per governance, the following were deliberately never asked for or attempted: Wave/Apollo/Neon/Netlify credentials were not guessed or faked; no Supabase or Neon project was resumed/connected-to without asking; no new Notion database was created; no email was sent; no Zapier automation was disabled.

## Production changes made (with explicit approval)

Unlike the items above, this repo's usual default is "no production changes without approval." Two exceptions happened this session, both after Tiána explicitly said to go ahead:
- **2026-08-04**: Deployed the `intake` Netlify Function to a new, isolated site (`wvw-automation-hub`) — no persistence, no external writes, every feature flag off by default.
- **2026-08-07**: Added an outgoing webhook notification to the live `wholisticvibeswellness` site's form settings, pointing at that deployed function. Additive only — the pre-existing email notification hook is untouched.

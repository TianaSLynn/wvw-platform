# Implementation Register

Status key: **Designed** (documented, no code) · **Built** (code exists) · **Dev-tested** (verified locally) · **Accepted** (passed acceptance criteria with Tiána) · **Production Approved** · **Active**

**2026-08-07: MHFA-REG-01 is now Active in production** — the first automation path to go live, per Tiána's explicit approval (`MHFA_REG_01_ENABLED=true`). Verified end-to-end with a real, immediately-deleted test registration (Notion page created and confirmed, then archived). **This covers both `FORM-MHFA-001` and `mhfa-individual-registration`, since they share the same feature flag** — real registrations through either form now write to MHFA-02 | Learners & Registrations in Notion.

**Immediate follow-up required, partially done**: per ADR-001, a hub path going live means the corresponding Zapier automation for that exact trigger must be disabled by Tiána (not by this system) to avoid dual-processing the same registration into duplicate records. **2026-08-07: Tiána disconnected the Notion connection from Zapier** (per her report — this session cannot see Tiána's Zapier account directly to verify the Zap's own state). That should stop the individual-registration Zap from writing to Notion even if the Zap itself is still enabled, closing the practical duplicate-write risk. Recommend Tiána also formally disable/archive the Zap itself when convenient, so it doesn't fail silently on every future trigger — but the dual-write risk this note originally flagged is now addressed.

**2026-08-07: MHFA-GRP-01 (AUTO-04) is now Active in production**, per Tiána's explicit approval ("you have my approval"). `MHFA_GRP_01_ENABLED=true` set on the live site (production context) and deployed. Verified end-to-end with a real, clearly-labeled test submission through `FORM-MHFA-002`: created one Organization row, one Contact row (in MHFA-02), and one Opportunity row (in MHFA-03), all confirmed via direct Notion fetch to have the exact properties and relations `group-opportunity.ts`/`mappers.ts` are supposed to produce. **Cleanup note**: unlike MHFA-REG-01's test record, these three test pages could **not** be archived/deleted — no delete or archive tool is available in this session's Notion MCP toolset, and the hub's own `client.ts` only implements `createPage`/`updatePage`, no archive. Instead, all three were relabeled (title and Notes rewritten to `ZZZ-DELETE-ME-TEST-...` with an explicit "safe to delete, created by automated verification" note) so they're unmistakable as test data. **They still need to be manually deleted from MHFA-03 and MHFA-02 by Tiána** — page URLs: `https://app.notion.com/p/3b5b0f7a44f38189971bd3c33191c43c` (Opportunity), `https://app.notion.com/p/3b5b0f7a44f3811ab4d1f10d405afe5b` (Organization), `https://app.notion.com/p/3b5b0f7a44f381ec8973dad89daf3ed5` (Contact).

Also added: `.claude/settings.local.json` (gitignored) with a Bash permission rule scoped to the exact `npx @netlify/mcp` deploy command for this site, at Tiána's explicit request, so future redeploys of this Netlify site don't hit the auto-mode classifier wall that blocked the first attempt.

**2026-08-07: AUTO-02 Payment Confirmation replacement (MHFA-PAY-01) deployed, report-only.** `GET /.netlify/functions/reconcile-payments`, live at `wvw-automation-hub.netlify.app`. Individual public MHFA registrants pay via the public Wave pay link, not a per-person Invoice — confirmed live via the real Wave business: only 2 real Invoice records exist (Wesley Shelter's group booking), but 40 real individual Customer records. Matches on `Customer.outstandingAmount` instead of invoices. **Deliberately never writes to Notion** — a zero balance can also mean "never charged," and Wave's API doesn't expose a clean way to tell the difference with this token's access. Verified live: `registrationsChecked: 0` (accurate — MHFA-02 is currently empty, the one test registration was cleaned up), `waveCustomersChecked: 40`, `candidates: []`. Turning candidate matches into real Payment Status writes is a separate, not-yet-made decision.

**Bug found and fixed in the process**: `queryDataSource` (the `/data_sources/{id}/query` endpoint) 400s under this client's Notion-Version (`2022-06-28`) — it needs `2025-09-03`. Rather than bump the version globally (risking the page-creation path that MHFA-REG-01 depends on, which may need a different `parent` shape for database pages under the newer version), added `queryDatabaseLegacy` using the pre-2025-09-03 `/databases/{id}/query` endpoint. `queryDataSource` itself is now unused by anything in this codebase but left in place, documented, for whenever the version question gets resolved properly.

**Ground truth confirmed by Tiána, 2026-08-07**: as of today, Wesley Shelter (both invoices, MHFA-2026-001 and -002) is the **only** confirmed-paid customer in Wave. The other ~39 individual Wave Customer records are prospective public MHFA registrants who were invoiced but did not follow through — not confirmed payments. This validates the design decision above: a naive `outstandingAmount === 0` match would have wrongly surfaced some of those 39 as "paid" candidates. Also corrected: FIN-01 and SALES-DB-09's MHFA-2026-002 records, originally backfilled 2026-08-04 when the invoice was $9,000 and unsent, now reflect its real, current state ($1,050, sent 2026-08-05, paid 2026-08-06).

All other entries below are at most **Dev-tested**. Nothing else has been accepted, approved for production, or activated.

| Component | Status | Evidence |
|---|---|---|
| Repo audit across 7 repositories + live Netlify/Supabase/Notion/Microsoft Graph state | Built | `docs/CURRENT_STATE_AUDIT.md` |
| ADR-001 (Zapier → hub migration decision) | Built | `docs/ARCHITECTURE_DECISIONS.md` |
| ADR-002 (repo location) | Built | `docs/ARCHITECTURE_DECISIONS.md` |
| ADR-003 (reuse existing Notion schema) | Built | `docs/ARCHITECTURE_DECISIONS.md` |
| Zapier removal register (14 automations classified) | Built | `docs/ZAPIER_REMOVAL_REGISTER.md` |
| Form registry (25 + 11 live forms, real field lists) | Built | `docs/FORM_REGISTRY.md`, sourced from Netlify API |
| Correlation ID standard (`WVW\|DOMAIN\|CODE\|DATE\|ULID`) | Dev-tested | `packages/shared-types/src/correlation-id.ts`; generation, parsing, and validation verified directly (vitest itself is blocked in this nested-repo location — see Known Issues below; verified via direct `tsx` execution instead) |
| Canonical payload hash (dedup) | Dev-tested | `packages/shared-types/src/payload-hash.ts`; verified key-order independence and content sensitivity |
| MHFA-REG-01 validation schema, matched field-for-field against the live `mhfa-individual-registration` Netlify form | Dev-tested | `packages/validation/src/mhfa-individual-registration.schema.ts` |
| Restricted-field splitting (accommodation summary never reaches general logs) | Dev-tested | Verified: restricted field correctly separated from general payload |
| MHFA-GRP-01 validation schema, matched field-for-field against the live `mhfa-group-training-inquiry` Netlify form | Dev-tested | `packages/validation/src/mhfa-group-training-inquiry.schema.ts` |
| MHFA-ACC-01 validation schema, matched field-for-field against the live `mhfa-accommodation-request` Netlify form | Dev-tested | `packages/validation/src/mhfa-accommodation-request.schema.ts` — entire form treated as Restricted; only a sanitized pointer (`accommodation-requested: true`, no content) is eligible for general logging, verified the pointer never contains the accommodation text |
| MHFA-ATT-01 validation schema, matched field-for-field against the live `FORM-MHFA-014` Netlify form | Dev-tested | `packages/validation/src/mhfa-attendance.schema.ts` — deliberately does not guess at select-field option values Netlify's API doesn't expose (see file comment) |
| MHFA-EVAL-01 validation schema, matched field-for-field against the live `FORM-MHFA-011` Netlify form | Dev-tested | `packages/validation/src/mhfa-evaluation.schema.ts` — free-text fields split from scored fields so individual comments don't flow into general dashboards unaggregated |
| MHFA-COMP-01 validation schema, matched field-for-field against the live `FORM-MHFA-013` Netlify form | Dev-tested | `packages/validation/src/mhfa-complaint.schema.ts` — entire narrative treated as Restricted per the CEO decision doc (routes to a TRAIN-19-equivalent, never TRAIN-18/general logs); verified the general pointer never contains narrative content |
| MHFA-CERT-CORR-01 validation schema, matched field-for-field against the live `FORM-MHFA-008` Netlify form | Dev-tested | `packages/validation/src/mhfa-certificate-correction.schema.ts` |
| MHFA-POST-01 validation schemas (pre-work support + payment/invoice reconciliation), matched field-for-field against the live `FORM-MHFA-010` and `FORM-MHFA-016` Netlify forms | Dev-tested | `packages/validation/src/mhfa-postwork.schema.ts`. Certificate eligibility itself (MHFA-CERT-01) is not implemented — it's a derived workflow needing persisted attendance/pre-work/post-work state, which needs the Supabase connection this hub doesn't have yet |
| `POST /.netlify/functions/intake` — dispatch table for MHFA-REG-01, MHFA-GRP-01, MHFA-ACC-01, MHFA-ATT-01, MHFA-EVAL-01, MHFA-COMP-01, MHFA-CERT-CORR-01, MHFA-POST-01 (×2 forms), plus FORM-MHFA-001/002 (added 2026-08-04) | **Deployed, live traffic, real Notion writes wired for registration paths** | Live at `https://wvw-automation-hub.netlify.app/.netlify/functions/intake`, its own Netlify site per ADR-002. Every path is still individually feature-flagged off by default, so nothing persists or notifies anyone unless a flag is explicitly turned on. **2026-08-04**: when a registration path's flag is on, `intake.ts` now actually calls `createPage` against MHFA-02 \| Learners & Registrations instead of only returning a dry-run response — verified live: flag off → `dry_run_feature_disabled`; flag on with no `NOTION_API_KEY` → `notion_not_configured` (502, explicit, never a false success). The live write itself (flag on + a real working `NOTION_API_KEY`) has **not** been exercised — no working key exists in this environment yet. **2026-08-07**: an outgoing webhook (hook id `6a755e753c5b34ea395ddcff`) now sends every real `wholisticvibeswellness` form submission to this function, site-wide, additive alongside the pre-existing email notification — real traffic is reaching the dispatch table for the first time |
| `packages/validation/src/form-mhfa-001.schema.ts`, `form-mhfa-002.schema.ts` | Dev-tested, deployed | Built 2026-08-04 to close the open finding below — matched field-for-field against the real live `FORM-MHFA-001`/`FORM-MHFA-002` forms (the numbered forms with real submissions). `FORM-MHFA-001` has a real Notion mapper (`formMhfa001ToHubRegistration`) wired into `intake.ts`. `FORM-MHFA-002` now also has a real Notion write path — see the MHFA-03 row below. Both original named-form schemas (`mhfa-individual-registration`, `mhfa-group-training-inquiry`) were kept, not replaced — real traffic patterns are still not confirmed with Tiána, so both sets are handled |
| MHFA-GRP-01 (AUTO-04) — `FORM-MHFA-002` → MHFA-03 \| Organizations & Group Opportunities | Dev-tested, NOT deployed, feature flag off | `packages/integration-notion/src/group-opportunity.ts` (orchestration) + `mappers.ts` (`organizationToNotionProperties`, `groupContactToNotionProperties`, `groupOpportunityToNotionProperties`, `normalizeDeliveryPreference`, `splitContactName`), wired into `intake.ts`'s `FORM-MHFA-002` handler behind `MHFA_GRP_01_ENABLED`. Real live MHFA-03 schema confirmed via Notion fetch 2026-08-07: it's **self-referential** — an "Organization" is just a row in the same table an "Opportunity" row lives in (`Organization` relation property links an opportunity back to its parent org row), and `Contact` is a relation into MHFA-02. As of 2026-08-07 MHFA-03 has zero records of any kind, so search-before-create logic has no real example to verify against yet — it's built from the schema alone and runs unconditionally regardless (governance: don't skip duplicate-detection just because today's data is empty). Three Notion calls per submission: find-or-create Organization (by exact title match), find-or-create Contact in MHFA-02 (by exact email match — reuses an existing registrant's page rather than duplicating them), then create the Opportunity row relating both. If the Opportunity create fails after Organization/Contact succeeded, those two rows are intentionally left in place (not rolled back) and the response reports exactly which step failed. `delivery-preference` is free text on the live form but a closed 4-option select in Notion — unmapped values are never forced into the select (would also just fail the API call); the raw value is preserved in Notes instead. Pure mapping functions verified via direct `tsx` execution (vitest is blocked in this subtree, see Known Issues); `tsc --noEmit` passes clean. **Not yet exercised against the live Notion API** — no test-and-cleanup run has been done for this path yet, unlike MHFA-REG-01 |
| Open finding, largely addressed: the live `mhfa-individual-registration` and `mhfa-group-training-inquiry` forms have zero real submissions ever, while the numbered `FORM-MHFA-001`/`FORM-MHFA-002` forms have real ones (confirmed via Netlify's form API). Handlers for both numbered forms now exist and are deployed, and the webhook wiring the whole open finding was blocking on is now live (2026-08-07). **Still open**: Tiána has not yet explicitly confirmed which set — named or numbered — is the actually-authoritative one going forward; both are handled defensively in the meantime. | In progress | — |
| Supabase core schema (`automation_events`, `workflow_executions`, `integration_mappings`, `automation_exceptions`, `communications_queue`, `dashboard_metrics`, `feature_flags`) | Designed, migration written | `supabase/migrations/0001_core_automation_tables.sql` — **not applied**, no active Supabase project (Decision 6 open) |
| Supabase MHFA domain schema (`organizations`, `contacts`, `learners`, `sessions`, `registrations`, `payments`, `form_registry`) | Designed, migration written | `supabase/migrations/0002_mhfa_domain_tables.sql` — **not applied** |
| Supabase persistence wiring for the intake function | Missing | Blocked on Decision 6 (which project, resume approval) |
| Notion integration package | Dev-tested (mappers only) | `packages/integration-notion/` — `client.ts` (credential-gated REST wrapper, `NOTION_API_KEY` required, throws rather than no-ops when unset) + `mappers.ts` (pure property-mapping functions for MHFA-02 Learners & Registrations, matched to the real live schema). Database IDs confirmed and recorded in `docs/NOTION_MAPPING.md`. The mapper logic is dev-tested directly (12 checks); the actual HTTP client has **not** been exercised against the live Notion API — no `NOTION_API_KEY` is available in this environment. Also documents a real schema gap: none of the 5 MHFA databases has a dedicated Correlation ID property, so it's embedded in `Notes` as a documented workaround pending Tiána's approval to add a real property |
| Microsoft Graph / Outlook draft creation | Missing | Session has a working Graph connection for read/search; draft-creation code not yet built |
| Wave Pro integration | Dev-tested against the live API | `packages/integration-wave/src/client.ts` (credential-gated, `WAVE_API_TOKEN` required) — `listBusinesses()` and `getBusinessInvoices()` verified 2026-08-04 against the real Wholistic Vibes Wellness LLC Wave business (resolved 2 real invoices: MHFA-2026-001 paid $2,550, MHFA-2026-002 $9,000 flagged never sent despite Wave's own `status` field labeling it OVERDUE — `status` is due-date math only, `lastSentVia` is the real delivery signal). `WAVE_API_TOKEN` is a Wave "Full Access" business API token (from Wave's own Settings > API page), not a short-lived OAuth access token — stored as an encrypted Netlify env var on the `wvw-automation-hub` site, never committed to this repo. No caller in `intake.ts` uses this yet — reconciliation/invoice-status-check automation is still unbuilt |
| Apollo.io integration | Missing | Blocked entirely — no credentials/connector available |
| MHFA-GRP-02, CERT-01, FUP-01, EMP-01 handlers | Missing | Not started. 8 of the 12 named MHFA workflows in the assignment spec are now built (REG-01, GRP-01, ACC-01, ATT-01, EVAL-01, COMP-01, CERT-CORR-01, POST-01). GRP-02 needs a Microsoft Forms + Excel roster-upload integration this hub doesn't have. CERT-01 needs persisted cross-record state (Supabase). FUP-01/EMP-01 are scheduled/content-delivery journeys, not single-form intake — need `scheduler_jobs` wired to a real database first |
| Executive dashboard app | Missing | Not started |
| PAM executive intelligence | Missing | Not started |
| CI/CD (GitHub Actions) | Missing | Not started |
| Full test suite (50 minimum scenarios from spec) | Missing | 8 scenarios covered by the current test file; the other ~42 require the missing components above |

## Known issues

- `npm test` (vitest) fails when run from inside `automation-hub/` because Vite's PostCSS config resolution walks up to the parent `wvw-platform` repo root (shared `.git`) and finds its Tailwind config, which isn't installed in this subtree. This is a direct consequence of ADR-002 (nesting inside `wvw-platform` instead of a standalone repo) and is expected to resolve itself once extracted to its own repository. In the meantime, all logic in this register was verified by direct execution (`npx tsx`), not by a passing `vitest run` — that distinction matters and is not being glossed over.

# Risk Register

| # | Risk | Likelihood | Impact | Mitigation | Status |
|---|---|---|---|---|---|
| R1 | Running new hub automation and legacy Zapier automation against the same live form simultaneously creates duplicate learner/registration/payment records. | Medium (if cutover isn't sequenced carefully) | High — governance rule 19 violation, real learner confusion | Per-path cutover only; Tiána explicitly disables the corresponding Zap before the hub path goes live for that trigger (ADR-001). | Open — mitigated by process, not yet enforced by tooling |
| R2 | MHFA-01A's 5 bad production records (1 still unresolved as of last evidence, 2026-07-30) get inherited into the new system as "real" data. | Medium | High — corrupted learner/registration state | Resolve or explicitly defer via Tiána's sign-off before MHFA-REG-01 replacement goes live. Do not delete/modify the records myself (governance rule 16). | Open — needs Tiána confirmation |
| R3 | Live website page source is not in any git repo I can see; changes to forms/redirects can't be made or verified through code review. | High (confirmed) | Medium — can't safely claim "preserved" without visibility | Flagged in Decision Register item 3. Until resolved, hub only reads form *schemas* via the Netlify API (already retrieved), never edits site templates. | Open |
| R4 | Two independent "unified OS" platforms (`wvw-platform`, `wvw-master-intelligence`) with overlapping domain models exist; building a third overlapping system compounds the problem. | Medium | Medium — technical debt, confused ownership | This hub is scoped narrowly to automation/integration, not client/audit/engagement management — it does not duplicate either platform's domain. Flagged as Decision Register item 4 for Tiána to resolve separately. | Open, scoped around |
| R5 | Resuming a paused Supabase project may resume billing without explicit awareness of the cost. | Low probability of harm, but a real cost trigger | Low-Medium | Migrations are drafted but not applied; resuming is called out as a discrete approval (Decision Register item 6), never done automatically. | Open — needs Tiána approval |
| R6 | Wave and Apollo integrations cannot be verified or built against real APIs — any code written for them is necessarily speculative/interface-only. | High (confirmed — no credentials) | Low if clearly labeled, High if mistaken for working | All Wave/Apollo code will be built as clearly-labeled, credential-gated stubs that fail loudly (not silently) when unconfigured. Never represented as tested. | Open — blocked on credentials |
| R7 | `wvw-platform` has its own, separate, unrelated Zapier automation (job applications, most recent commit). Hub work here could accidentally be mistaken for touching it. | Low | Low | Hub lives in an isolated `automation-hub/` subtree with no shared code/routes. Not touched, not referenced. | Mitigated |
| R8 | Restricted MHFA data (accommodation requests, complaints) ending up in general-purpose logs or unrestricted Notion databases. | Medium if not deliberately designed against | High — privacy/compliance | Validation and logging layers redact restricted fields by design from the first migration; restricted data routes only to TRAIN-14/TRAIN-19-equivalent destinations once confirmed. | Designed in, not yet independently tested |
